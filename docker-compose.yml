services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: referenceapp
      POSTGRES_USER: referenceapp
      POSTGRES_PASSWORD: changeme
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
